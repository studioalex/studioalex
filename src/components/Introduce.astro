---
import Headline from "./Headline.astro"
import Ruler from "./Ruler.astro"

export interface ServiceType {
  title: string,
  description: string
}

export interface Props {
	email: string,
  profile: string,
  services?: ServiceType[]
  usesPath: string
}

const { email, profile, services, usesPath } = Astro.props;
---
<div class="sa-introduce">
  <Ruler />
  <div set:html={profile}></div>
  <p>
    I can help your team with:
  </p>
  <ul>
    { services.map(service => (
      <li>
        <strong>{service.title}</strong>: {service.description}
      </li>
    ))}
  </ul>
  <p>
    If this sounds like something you're interested in, send me an
    <a href={`mailto:${email}`}>email</a>!
  </p>

  <Ruler />
  <Headline>Uses</Headline>
  <p>
    I am often asked about the specifics of some piece of software or hardware I use,
    so <a href={usesPath}>this page</a> will serve as a living document and a place to point curious designers and developers
    to when I get asked.
  </p>
  <p>
    Also, take some time and check out <a href="https://uses.tech" target="_blank">//uses.tech</a> where many other great developers show what they are using.
  </p>
</div>

<style is:global>
.sa-introduce {
  max-width: var(--screen-small);
  margin: 2rem auto;
  color: var(--color-text-highlight);

  p {
    font-size: var(--font-size);
    font-weight: 300;
    margin: 0 0 1.5rem;
    line-height: 1.5;
  }

  ul {
    font-size: var(--font-size);
    font-weight: 300;
    padding: 1rem 0;

    li {
      margin-bottom: 1rem;
      margin-left: 1rem;
      line-height: 1.5;
      color: var(--color-text);
    }

    strong {
      font-weight: 400;
      color: var(--color-text-highlight);
    }
  }
}
</style>
