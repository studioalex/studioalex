---
import Icon from '../atoms/Icon.astro'
import ButtonUses from '../ButtonUses.astro'

export interface SocialType {
  icon: string;
  url: string;
}

export interface Props {
	list: SocialType[],
  uses?: string,
  usesLink?: string
}

const { list, uses, usesLink } = Astro.props;
---
<div class="abc-social">
  {list.map(social => (
    <a
      href={social.url}
      class="abc-social-link"
      target="_blank">
      <Icon
        class="abc-social-svg"
        icon={social.icon} />    
    </a>
  ))}
</div>
<div class="abc-social">
  {(uses && usesLink) && <ButtonUses link={usesLink} name={uses} /> }
</div>

<style>
.abc-social {
  padding: 2rem var(--margin-sides);
  text-align: center;
      
  &-link {
    display: inline-block;
    padding: 0.6rem;
    margin-right: 1rem;
    color: var(--theme-color-text);
    border-radius: 6px;
    
    &:hover {
      color: var(--theme-color-text--highlight);
    }

    svg {
      width: 40px;
      height: 40px;
    }
  }
      
  &-svg {
    width: 40px;
    height: 40px;
  }

  svg {
    width: 40px;
    height: 40px;
  }
}
</style>
